[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/PyQuiver"]


[project]
name = "PyQuiver"
version = "0.1.0"
dependencies = [
  "numpy",
]